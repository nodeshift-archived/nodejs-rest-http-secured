// configuration for the openshift test

// Fill in the authentication data
const config = {
  grant_type: 'password',
  realm: 'master',
  username: '*',
  password: '*',
  client_id: '*',
  client_secret: '*'
};

module.exports = config;
